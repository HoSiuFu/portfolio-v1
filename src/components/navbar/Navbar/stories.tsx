import { Meta, StoryObj } from '@storybook/react'
import Navbar from '.'

const meta: Meta = {
    title: 'Atoms/Navbar/Navbar',
    component: Navbar,
}

export default meta

export const Default: StoryObj = {}
