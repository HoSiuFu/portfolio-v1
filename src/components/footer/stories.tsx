import Footer from '.'
import { Meta, StoryObj } from '@storybook/react'

const meta: Meta = {
    title: 'Atoms/Footer',
    component: Footer,
}

export default meta

export const Default: StoryObj = {}
